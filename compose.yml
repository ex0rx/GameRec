services:
  api:
    build: .
    ports:
      - "8000:8000"

    volumes:
      - .:/app

    command:
      - uv
      - run
      - --no-sync
      - uvicorn
      - gamerec.main:app
      - --host
      - "0.0.0.0"
      - --port
      - "8000"
      - --reload